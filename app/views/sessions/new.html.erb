<!DOCTYPE html>
<html>
    <head>
<meta charset="utf-8">
 <%= stylesheet_link_tag    'style', media: 'all' %>
</head>
<body>
<h1>EL BARO</h1>
<h2>Transacciones seguras</h2>
<div class="section">
  <%=image_tag("user.png")%>

     <%= form_for(:session, :html => {class: "form-horizontal", role: "form"}, url: login_path) do |f| %>
            <div class='form-group'>
            <div class="control-label col-sm-2">
                <%= f.label :usuario %>
                 </div>
            <div class='col-sm-8'>
                <%= f.text_field :usuario, id: 'correo', placeholder: 'Ingresa usuario', autofocus: true %>
                </div>
                </div>

                 <div class='form-group'>
            <div class="control-label col-sm-2">
                <%= f.label :password %>
                </div>
            <div class='col-sm-8'>
                <%= f.password_field :password,id: 'contraseña', placeholder: 'Ingresa contraseña' %>
                 </div>
                </div>
            <div class='form-group'>
            <div class='col-sm-offset-2 col-sm-10'>
                <!-- Si el usuario no esta registrado el boton dira SIGN UP-->
                <!-- Pero si ya esta registrado entonces dira UPDATE -->
                <%= f.submit "Log in", id: 'boton'%>
                </div>
                </div>

            <% end %>
        <div class='col-xs-4 col-xs-offset-4'>
            [ <%= link_to "Regresar" , root_path%> ]
        </div>

</div>

</body>
</html>
